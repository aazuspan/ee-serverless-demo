name: pulumi-ee-demo
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: A minimal Google Cloud Python Pulumi program
config:
  pulumi:tags:
    value:
      pulumi:template: gcp-python
